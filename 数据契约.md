# AI 娘表情包站数据契约

版本：`v0.2`

## 1. 产品边界

本站收集不同 AI 角色的二创表情包，支持通过 GitHub Issue 投稿。前端不展示完整 tag 分类列表，tag 只作为搜索依据。每张作品有独立详情页，评论由第三方服务（GitHub Discussions / Giscus）承载，不进本站数据。

## 2. 表情包记录

```json
{
  "id": "sticker_01J8Y7K3Q2",
  "name": "这合理吗",
  "description": "用于表达质疑或无语的表情包",
  "path": "assets/deepseek/这合理吗.webp",
  "thumbnailPath": "previews/deepseek/这合理吗.webp",
  "format": "webp",
  "mimeType": "image/webp",
  "isAnimated": false,
  "width": 512,
  "height": 512,
  "fileSize": 183420,
  "sha256": "...",
  "characterId": "deepseek",
  "tags": ["吐槽", "疑问"],
  "submitter": {
    "name": "username",
    "github": "username"
  },
  "origin": {
    "type": "self-created",
    "sourceUrl": null,
    "author": "username",
    "note": "本人使用 AI 生成"
  },
  "license": {
    "type": "submitter-permission",
    "note": "投稿者确认允许本站收录和下载"
  },
  "status": "published",
  "createdAt": "2026-09-15T10:00:00+08:00",
  "updatedAt": "2026-09-15T10:00:00+08:00"
}
```

## 3. 字段定义

| 字段 | 类型 | 必填 | 生成方式 | 说明 |
|---|---|---:|---|---|
| `id` | string | 是 | 系统生成 | 全局唯一 ID，不使用文件名作为 ID |
| `name` | string | 是 | 投稿者填写 | 图片展示名称 |
| `description` | string | 否 | 投稿者填写 | 简短描述，用于搜索和图片替代文本 |
| `path` | string | 是 | 系统生成 | 仓库内相对路径 |
| `thumbnailPath` | string | 是 | 系统生成 | 瀑布流使用的缩略图路径，原图弹窗使用 `path` |
| `format` | string | 是 | 系统生成 | png、jpg、jpeg、gif、webp、apng 等 |
| `mimeType` | string | 是 | 系统生成 | 文件 MIME 类型 |
| `isAnimated` | boolean | 是 | 系统生成 | 是否为动态图片 |
| `width` | integer | 是 | 系统生成 | 图片宽度，单位 px |
| `height` | integer | 是 | 系统生成 | 图片高度，单位 px |
| `fileSize` | integer | 是 | 系统生成 | 文件大小，单位 byte |
| `sha256` | string | 是 | 系统生成 | 用于检测重复文件 |
| `characterId` | string | 是 | 投稿者选择 | 角色 ID |
| `tags` | string[] | 否 | 投稿者填写 | 自由 tag，不做前端分类按钮 |
| `submitter` | object | 是 | 投稿者填写 | 提交者信息 |
| `origin` | object | 是 | 投稿者填写 | 内容来源信息 |
| `license` | object | 是 | 投稿者确认 | 收录和下载授权状态 |
| `status` | string | 是 | 系统/管理员 | 审核和展示状态 |
| `createdAt` | datetime | 是 | 系统生成 | 首次收录时间 |
| `updatedAt` | datetime | 是 | 系统生成 | 最近修改时间 |

## 4. 角色记录

```json
{
  "id": "deepseek",
  "name": "DeepSeek娘",
  "aliases": ["蓝色大肥鱼", "鲸鱼娘"],
  "status": "active"
}
```

角色名称可以修改，图片只保存 `characterId`。搜索角色别名时仍能找到对应图片。

## 5. 来源类型

`origin.type` 支持：

```text
self-created       自己创作或生成
author-submitted    原作者本人投稿
internet-found      网络整理
community-created   社区成员创作
unknown             来源不明
```

`sourceUrl` 是可选字段。自己生成或没有公开来源的内容可以填写 `null`，不能因为没有链接而无法投稿。

## 6. 授权类型

```text
submitter-permission  投稿者确认允许收录和下载
author-permission     原作者明确授权
cc0                   CC0
cc-by                 CC BY
cc-by-nc              CC BY-NC
unknown               授权状态不明
removed                已撤下
```

投稿者必须确认：自己有权投稿，或本站收到版权投诉时可以联系投稿者协助处理。

## 7. 状态

```text
pending    等待审核
published  已公开展示
rejected   审核拒绝
removed    已下架
```

只有 `published` 记录进入公开前端数据。

## 8. 搜索契约

默认使用模糊搜索，不要求用户输入完整词语，也不区分大小写。

搜索范围：

- `name`
- `description`
- `tags`
- 角色 `name`
- 角色 `aliases`
- 投稿者名称

搜索前统一标准化：去除大小写差异、空格、下划线和短横线。

示例：搜索“`大肥鱼`”可以命中“蓝色大肥鱼”“DeepSeek娘大肥鱼”和“吃白饭的大肥鱼”。

## 9. 前端筛选与排序

前端支持：

- 关键词模糊搜索
- tag 模糊搜索
- 角色筛选
- 格式筛选
- 静态/动态筛选

排序支持：

- 最新收录
- 最早收录
- 名称正序
- 名称倒序
- 文件大小
- 随机

暂不实现情绪、场景、颜色等复杂分类，也不展示 tag 云或完整 tag 按钮列表。排序不包含任何热度维度（见 §11）。

列表按批次加载：首批 24 条，滚动接近底部时继续追加，直到当前筛选结果全部展示完。
因此前端会一次性持有全部 `published` 记录并按筛选结果分页，不要求接口分页；
若将来记录量级变大，再把这个批次加载改成真正的分页接口（`limit` / `offset`）。

## 10. 投稿校验

自动校验：

- 文件格式是否受支持；
- 文件大小是否超过限制；
- 是否存在相同 `sha256`；
- `characterId` 是否存在；
- `name` 是否为空；
- `tags` 是否为字符串数组；
- 图片是否能正常解析；
- 是否包含可执行文件或异常扩展名。

投稿通过自动校验后进入 `pending`，管理员审核后才变为 `published`。

## 11. 不做热度统计

`v0.2` 起移除 `downloadCount` 字段及其排序，本站不再统计任何热度指标。

不统计、不存储、不展示：

- 浏览量（PV / UV）；
- 点赞、收藏、分享；
- 下载次数；
- 任何形式的热度分数或排行榜。

原因：

- 站点是纯静态的，没有后端可以累计计数，前端自增的数字没有意义也无法防刷；
- 热度排序会让冷门作品得不到曝光，与"按角色归档的开放档案"定位不符；
- 不做统计也就不需要追踪脚本、Cookie 或用户标识，站点可以保持零追踪。

需要"发现作品"时使用：按角色浏览、Tag 检索、随机排序。

## 12. 评论

评论不属于本站数据契约：

- 评论内容托管在第三方服务（默认 Giscus，基于 GitHub Discussions）；
- 讨论串与作品的对应关系由前端按 `sticker-<id>` 作为 term 建立，不需要在作品记录里存字段；
- 本站不保存评论、不保存评论者身份，也不把评论数量写回作品记录；
- 切换评论服务只影响前端，作品记录不变。

## 13. 变更记录

| 版本 | 日期 | 变更 |
|---|---|---|
| `v0.2` | 2026-09-15 | 移除 `downloadCount` 与下载量排序；新增 §11 不做热度统计、§12 评论；排序新增"名称倒序"，"随机"改为会话内稳定顺序 |
| `v0.1` | 2026-09-15 | 首版：作品记录、角色记录、来源与授权类型、搜索与筛选、投稿校验 |
